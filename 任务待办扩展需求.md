# 任务待办扩展需求

## 1. 背景

当前任务模块只支持简单的待完成/已完成分组、创建、编辑、完成和删除。它适合记录很轻量的事项，但当任务数量变多、任务有截止时间、优先级、项目归属或执行状态时，现有能力不足。

本次扩展目标是：在保持当前轻量浮窗体验的基础上，让任务模块从“简单清单”升级为“可管理的个人待办工作台”。

## 2. 优化目标

- 支持更完整的任务信息，而不只是单行文本。
- 支持任务详情页，用于编辑任务标题、备注、日期、优先级和标签。
- 支持按状态、日期、优先级筛选任务。
- 支持任务的快速录入和快速完成。
- 支持更清晰的任务卡片展示，方便一眼判断哪些任务更重要。
- 保持任务模块离线可用，继续使用本地存储。
- 不影响笔记模块现有交互。

## 3. 页面结构

### 3.1 任务首页

任务首页保留当前主要结构：

- 顶部「笔记 / 任务」切换。
- 搜索任务输入框。
- 「新建任务」按钮。
- 任务状态切换。
- 任务列表。
- 空状态和搜索无结果状态。

任务首页需要扩展：

- 状态筛选：待完成、进行中、已完成、全部。
- 日期筛选：今天、近期、已逾期、无日期。
- 优先级筛选：高、中、低、无优先级。
- 任务卡片展示更多信息。

### 3.2 任务详情页

新建任务和编辑任务时进入独立任务详情页，不在首页内只做单行编辑。

任务详情页建议包含：

- 返回按钮。
- 删除按钮。
- 任务标题输入框。
- 任务备注输入区。
- 状态选择。
- 截止日期选择。
- 优先级选择。
- 标签输入/选择。
- 自动保存状态。

任务详情页仍在当前应用窗口内展示，不新开系统窗口。

## 4. 主要流程

### 4.1 新建任务

1. 用户点击「新建任务」。
2. 应用进入任务详情页。
3. 默认聚焦任务标题。
4. 用户输入标题、备注、日期、优先级等内容。
5. 输入过程中自动保存。
6. 如果标题为空且备注为空，返回时丢弃空任务。

### 4.2 编辑任务

1. 用户点击任务卡片或编辑入口。
2. 应用进入任务详情页。
3. 用户修改任务信息。
4. 修改过程中自动保存。
5. 点击返回或按 `Esc` 回到任务首页。

### 4.3 完成任务

1. 用户在任务首页点击任务左侧圆形完成控件。
2. 待完成任务切换为已完成。
3. 已完成任务再次点击可恢复为待完成。
4. 完成时间需要记录。

### 4.4 删除任务

1. 用户点击删除入口。
2. 使用当前图标二次确认交互。
3. 再次点击确认删除。
4. 删除后返回任务首页或从列表移除。

## 5. 任务字段

建议任务数据结构扩展为：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 任务唯一标识 |
| title | string | 任务标题 |
| notes | string | 任务备注 |
| status | string | `todo` / `doing` / `done` |
| priority | string | `high` / `medium` / `low` / `none` |
| dueDate | string/null | 截止日期 |
| tags | string[] | 标签 |
| createdAt | number | 创建时间 |
| updatedAt | number | 更新时间 |
| completedAt | number/null | 完成时间 |

兼容要求：

- 旧任务只有 `content` 字段时，应自动迁移为 `title`。
- 旧任务的 `completed: true` 应迁移为 `status: done`。
- 搜索应覆盖标题、备注和标签。
- 保存时保留旧字段兼容能力，避免旧数据丢失。

## 6. 任务卡片展示

任务卡片建议展示：

- 完成状态圆形控件。
- 任务标题。
- 优先级标记。
- 截止日期。
- 标签。
- 备注摘要。
- 逾期状态。

卡片展示规则：

- 高优先级任务需要有明显标识，但不要过度刺眼。
- 已逾期任务需要标记为逾期。
- 无备注、无日期、无标签的任务保持简洁。
- 已完成任务弱化显示。

## 7. 筛选与排序

### 7.1 状态筛选

- 待完成。
- 进行中。
- 已完成。
- 全部。

### 7.2 日期筛选

- 今天。
- 近期。
- 已逾期。
- 无日期。

### 7.3 排序规则

默认排序建议：

1. 已逾期任务。
2. 今天到期任务。
3. 高优先级任务。
4. 最近更新任务。

已完成任务按完成时间倒序。

## 8. 快速操作

任务首页应支持：

- 点击完成控件切换完成状态。
- 双击任务进入详情页，是否保留待确认。
- 点击任务标题进入详情页，是否启用待确认。
- 右侧显示编辑和删除入口。
- `Enter` 保存当前编辑。
- `Esc` 返回或取消当前编辑。

## 9. 视觉要求

- 继续保持当前浅灰背景、白色卡片、深色主按钮、圆角控件风格。
- 任务卡片应比笔记卡片更紧凑，适合快速扫描。
- 筛选控件不要占用过多高度。
- 优先级和日期信息要清晰，但不能让列表显得杂乱。
- 任务详情页应更像表单，不做富文本编辑器。

## 10. 非功能需求

- 任务数量较多时滚动和筛选不能明显卡顿。
- 本地存储失败时需要提示。
- 离线可用，不依赖外部网络。
- 旧任务数据必须正常迁移。
- 不影响笔记模块已有数据和交互。

## 11. 验收标准

- 可以创建包含标题、备注、优先级、截止日期、标签的任务。
- 可以编辑已有任务详情。
- 可以切换任务状态：待完成、进行中、已完成。
- 可以按状态、日期、优先级筛选任务。
- 可以搜索标题、备注和标签。
- 可以删除任务，并有二次确认。
- 已完成任务保留完成时间。
- 旧版本任务数据可以正常显示。
- 重启应用后任务数据不丢失。

## 12. 待确认问题

- 任务详情页是否和笔记编辑页一样使用独立页面？
- 点击任务卡片是进入详情，还是只选中？
- 是否保留双击任务进入详情？
- 是否需要任务备注支持 Markdown？
- 优先级是否只需要高/中/低/无？
- 截止日期是否需要具体时间，还是只需要日期？
- 是否需要提醒功能？
- 是否需要重复任务？
- 是否需要子任务/checklist？
- 是否需要标签管理页，还是仅支持任务内自由输入标签？
- 是否需要进行中状态，还是只保留待完成/已完成？
