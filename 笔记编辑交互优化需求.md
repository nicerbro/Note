# 笔记编辑交互优化需求

## 1. 背景

当前笔记的新建和编辑主要发生在首页列表区域内，适合快速记录短内容，但当笔记内容较长、需要整理结构、反复编辑或使用富文本时，编辑空间明显不足。

本次优化目标是：新建笔记和编辑笔记时进入独立的笔记编辑页面，让用户可以在更大的区域内编写内容，同时保留返回笔记首页的能力，并逐步支持更完整的富文本编辑。

## 2. 优化目标

- 新建笔记时，不再只在首页局部区域编辑，而是进入独立编辑页面。
- 编辑已有笔记时，从笔记卡片进入独立编辑页面。
- 编辑页面应能展示更多正文内容，减少受首页列表布局限制的问题。
- 编辑页面必须支持返回笔记首页。
- 编辑页面应支持基础富文本能力，方便用户记录结构化内容。
- 首页继续承担笔记列表、搜索、快速查看、删除、进入编辑等职责。

## 3. 已确认决策

- 新建笔记进入编辑页后，默认聚焦标题输入框。
- 保存策略采用自动保存。
- 返回首页时自动保存，不需要未保存内容二次确认。
- 编辑页面需要提供删除笔记入口。
- 首页点击笔记卡片进入预览状态。
- 首页点击编辑按钮才进入编辑状态。
- 不保留双击进入编辑的交互。
- 富文本底层格式优先采用 Markdown。
- 第一版支持上传图片。
- `Esc` 用于返回笔记首页。
- 编辑页面需要显示创建时间和更新时间。
- 图片上传后保存到笔记资源目录。
- Markdown 编辑器采用所见即所得模式。
- 首页卡片摘要需要渲染部分 Markdown 样式。
- 返回首页后不需要高亮刚编辑过的笔记。
- 预览状态和编辑状态使用同一个独立详情页面；默认进入预览态，点击编辑按钮后激活编辑。

## 4. 页面结构

### 4.1 笔记首页

笔记首页保留当前主要结构：

- 顶部「笔记 / 任务」切换。
- 置顶按钮和关闭按钮。
- 搜索笔记输入框。
- 「新建笔记」按钮。
- 笔记卡片列表。
- 空状态和搜索无结果状态。

首页行为调整：

- 点击「新建笔记」后进入独立详情页面的编辑态，并创建一条新笔记草稿。
- 点击已有笔记卡片后进入独立详情页面的预览态。
- 点击已有笔记的编辑按钮后进入独立详情页面的编辑态。
- 首页不再展开大面积的笔记编辑区域。
- 首页卡片只负责摘要展示，不承担完整正文编辑。
- 不再支持双击笔记卡片进入编辑。

### 4.2 笔记详情页面

笔记详情页面是预览态和编辑态共用的独立页面。用户从首页点击笔记卡片后进入该页面，默认处于预览态；点击编辑按钮后，同一页面切换为编辑态。

详情页面在预览态建议包含：

- 返回笔记首页的入口。
- 笔记标题。
- 创建时间和更新时间。
- Markdown 渲染后的正文内容。
- 编辑按钮。
- 删除按钮。

预览状态行为：

- 点击首页笔记卡片进入独立详情页面的预览态。
- 点击预览态中的编辑按钮后，在同一页面激活编辑态。
- 预览状态不展示富文本编辑工具栏。
- 预览状态中的删除操作需要沿用当前二次确认交互。

### 4.3 笔记编辑状态

编辑状态在当前详情页面内激活，不新开系统窗口。

编辑状态建议包含：

- 顶部返回按钮。
- 页面标题区域，用于显示「新建笔记」或「编辑笔记」状态。
- 创建时间和更新时间。
- 笔记标题输入框。
- 富文本工具栏。
- 大面积正文编辑区域。
- 保存状态提示。
- 删除笔记入口。
- 可选的更多操作入口，例如清空格式、复制内容。

编辑状态需要尽量利用当前窗口空间，正文区域应成为页面中最大的交互区域。

## 5. 主要流程

### 5.1 新建笔记流程

1. 用户在笔记首页点击「新建笔记」。
2. 应用进入独立详情页面，并直接激活编辑态。
3. 自动创建一条新笔记草稿。
4. 光标默认聚焦到标题输入框。
5. 用户输入标题和正文。
6. 用户输入过程中自动保存有效内容。
7. 用户点击返回或按 `Esc` 时，应用自动保存并回到笔记首页。
8. 如果标题和正文均为空，返回时应丢弃空草稿。

### 5.2 预览笔记流程

1. 用户在首页点击笔记卡片。
2. 应用进入独立笔记详情页面，默认处于预览态。
3. 预览态展示标题、创建时间、更新时间和 Markdown 渲染后的完整正文。
4. 用户点击编辑按钮后，在同一详情页面激活编辑态。
5. 用户点击返回或按 `Esc` 时回到笔记首页。

### 5.3 编辑笔记流程

1. 用户在首页点击笔记编辑按钮，或在预览态点击编辑按钮。
2. 应用进入独立详情页面的编辑态。
3. 编辑态加载该笔记的完整标题和正文。
4. 用户修改内容。
5. 修改过程中自动保存。
6. 用户点击返回或按 `Esc` 后回到笔记首页。
7. 首页列表展示更新后的笔记摘要和更新时间。

### 5.4 返回首页流程

返回入口应始终可见。

返回时需要处理：

- 已保存内容：直接返回首页。
- 正在输入的内容：自动保存后返回首页。
- 空的新建草稿：直接丢弃，不生成空笔记。

## 6. 富文本编辑能力

### 6.1 第一阶段必须支持

- 加粗。
- 斜体。
- 下划线。
- 删除线。
- 无序列表。
- 有序列表。
- 待办清单。
- 标题级别，例如正文、标题 1、标题 2、标题 3。
- 引用块。
- 分割线。
- 上传图片。
- 撤销和重做。

### 6.2 建议支持

- 插入链接。
- 行内代码。
- 代码块。
- 清除格式。
- 文本颜色。
- 背景高亮。
- 粘贴时保留基础格式。
- 粘贴纯文本。

### 6.3 暂不纳入第一阶段

- 附件管理。
- 表格编辑。
- 多端同步。
- 云端协作。

这些能力可以作为后续版本扩展。

## 7. 数据结构建议

当前笔记正文如果仍然只保存纯文本，将无法完整表达富文本格式。第一版富文本底层格式优先采用 Markdown，同时保留纯文本字段用于搜索和首页摘要。

建议字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 笔记唯一标识 |
| title | string | 笔记标题 |
| content | string | 纯文本内容，用于兼容旧数据和搜索 |
| markdownContent | string | Markdown 正文内容，用于编辑和渲染 |
| images | array | 笔记资源目录中的图片资源信息 |
| createdAt | number | 创建时间 |
| updatedAt | number | 更新时间 |

数据兼容要求：

- 旧笔记只有 `content` 时，应能正常打开编辑。
- 打开旧笔记后，可以将纯文本转换为 Markdown 初始内容。
- 搜索仍优先基于纯文本字段，避免富文本结构影响搜索结果。
- 首页摘要应渲染部分 Markdown 样式，例如加粗、列表、链接文本和基础段落结构，但不进入完整编辑状态。
- 图片应保存到笔记资源目录，不依赖外部网络地址。

## 8. 保存策略

保存策略采用自动保存。

- 用户输入后自动保存。
- 返回首页时无需额外点击保存。
- 页面显示「已保存」或「保存中」状态。
- 保存失败时需要显示错误提示。
- 自动保存需要做防抖，避免输入时频繁写入导致卡顿。
- 返回首页时如仍有未完成保存，应先完成保存再返回；保存失败时提示用户。

## 9. 交互细节

- 编辑页面顶部返回按钮应放在左上角，避免和关闭窗口按钮混淆。
- 窗口关闭按钮仍表示隐藏或关闭应用，不等同于返回首页。
- 标题为空时，首页显示「无标题」。
- 正文为空但标题不为空时，允许保存。
- 标题和正文都为空的新建笔记不保存。
- 编辑页面应支持键盘快捷键：
  - `Ctrl+B` 加粗。
  - `Ctrl+I` 斜体。
  - `Ctrl+U` 下划线。
  - `Ctrl+Z` 撤销。
  - `Ctrl+Y` 重做。
  - `Esc` 返回首页。
- 富文本工具栏按钮需要有选中态。
- 编辑区域滚动时，工具栏建议保持可见。
- 编辑页面需要展示创建时间和更新时间。
- 编辑页面的删除入口需要沿用当前二次确认交互。

## 10. 视觉要求

- 整体视觉继续沿用当前浅灰背景、白色内容区、圆角和深色主按钮风格。
- 编辑页面不做营销式布局，应保持轻量、专注、工具化。
- 正文编辑区域应宽敞，减少卡片嵌套。
- 工具栏图标应清晰，按钮尺寸适合鼠标点击。
- 页面切换应轻柔，不需要复杂动画。
- 返回首页后，不需要高亮刚编辑过的笔记。

## 11. 非功能需求

- 编辑长笔记时输入不能明显卡顿。
- 富文本内容保存后重新打开，格式不能丢失。
- 旧版本纯文本笔记必须能继续读取。
- 本地存储失败时应有错误提示。
- 应用离线可用，不依赖外部 CDN。
- 改动应保持当前 Tauri 桌面应用可打包。
- 图片上传、插入、保存和重新打开后必须可用。
- 所见即所得编辑器应保持输入流畅，不能因为 Markdown 转换造成明显卡顿。

## 12. 验收标准

- 点击「新建笔记」能进入独立详情页面的编辑态。
- 新建笔记进入编辑页后默认聚焦标题输入框。
- 点击首页笔记卡片进入独立详情页面的预览态。
- 点击编辑按钮在同一详情页面激活编辑态。
- 点击已有笔记能进入独立详情页面并显示完整内容。
- 编辑页面可以通过返回按钮或 `Esc` 返回笔记首页。
- 长内容在编辑页面中有足够空间展示和编辑。
- 至少支持加粗、斜体、列表、标题、待办清单、图片上传等基础富文本。
- 自动保存生效，返回首页不需要二次确认。
- 保存后回到首页，笔记标题、摘要、更新时间正确更新。
- 关闭并重新打开应用后，Markdown 内容和图片仍然存在。
- 图片保存到笔记资源目录。
- 首页卡片摘要能渲染部分 Markdown 样式。
- 返回首页后不高亮刚编辑过的笔记。
- 旧纯文本笔记可以正常打开、编辑和保存。
- 编辑页面展示创建时间和更新时间。
- 任务页功能不受本次改动影响。

## 13. 后续实现细节

- 笔记资源目录的具体路径和命名规则需要在实现时定义。
- 图片插入后的 Markdown 引用路径需要支持应用迁移和重新打包后仍可读取。
- 所见即所得编辑器的技术选型需要优先满足离线可用、打包体积可控和输入流畅。
- 首页卡片摘要需要定义支持渲染的 Markdown 子集，避免复杂内容挤压列表布局。
- 预览态和编辑态需要共享同一详情页面布局，但按钮、工具栏和输入能力要有清晰状态区分。
