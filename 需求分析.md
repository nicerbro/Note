# 浮空笔记需求分析

## 1. 背景与目标

浮空笔记是一个面向桌面端封装的轻量本地记录工具。产品以 Figma 中的轻量浮窗视觉为准，在一个固定尺寸浮窗中承载“笔记”和“待办”两类高频记录场景。

当前版本目标不是营销页面或纯展示 Demo，而是一个可直接打开、后续可包装成桌面应用的可用前端。数据需要本地持久化，当前浏览器版本使用 `localStorage`，后续桌面端可替换为本地 JSON 文件或应用内置存储。

## 2. 技术与形态结论

- 前端形态：单文件 `index.html`。
- 技术栈：原生 HTML、CSS、JavaScript。
- 依赖策略：不依赖 React、Tailwind、Babel、CDN 或构建流程。
- 视觉策略：优先保持 Figma 的浅灰圆角浮窗、分段切换、搜索栏、卡片和任务分组样式。
- 应用图标：使用与浮窗视觉一致的圆角纸页 + 勾选标识，作为浏览器 favicon 和后续桌面端图标源。
- 桌面端预留：顶部区域包含 Electron 兼容拖拽类；置顶按钮当前为视觉状态，真实系统置顶由后续桌面壳实现。
- 数据接口预留：支持可选的 `window.noteStorage.load()` 和 `window.noteStorage.save(data)`，供后续 Electron 后端接管本地文件保存。

## 3. 用户与场景

目标用户是在当前工作过程中需要快速记录信息的人。

典型场景包括：

- 快速记录会议要点、想法、资料片段。
- 创建短周期个人待办事项。
- 在笔记与任务之间切换，不离开当前工作上下文。
- 搜索已有笔记或任务。
- 修改、删除、完成或恢复任务。

## 4. 整体界面需求

应用以一个浮窗作为主界面，包含：

- 顶部“笔记 / 任务”分段切换。
- 顶部右侧置顶按钮和关闭按钮。
- 搜索输入框。
- 根据当前模式显示“新建笔记”或“新建任务”按钮。
- 主内容区根据当前模式展示笔记列表或任务列表。

通用交互：

- 点击“笔记”进入笔记模式。
- 点击“任务”进入任务模式。
- 当前模式必须有明显选中态。
- 点击关闭按钮隐藏浮窗。
- 关闭后显示“打开浮空笔记”按钮，可重新打开。
- `Ctrl+Shift+N` 可显示或隐藏应用。
- 置顶按钮点击后切换视觉状态。
- 鼠标长按或拖动顶部栏区域时，可移动浮窗位置；短按顶部按钮仍执行对应点击操作。

## 5. 笔记功能需求

### 5.1 新建笔记

- 点击“新建笔记”后，在列表顶部显示笔记编辑区。
- 编辑区包含标题输入框、内容输入框、取消按钮、保存按钮。
- 标题为空时保存为“无标题”。
- 标题或内容至少有一项不为空才保存。
- 保存成功后，新笔记进入笔记列表顶部。
- 点击取消后放弃当前未保存内容。

### 5.2 编辑笔记

- 每条笔记支持点击编辑图标进入编辑状态。
- 每条笔记支持双击卡片进入编辑状态。
- 编辑已有笔记时，应在原笔记卡片所在位置内联修改，不在列表顶部额外生成编辑框。
- 编辑状态复用标题和内容输入区。
- 点击保存后更新原笔记。
- 点击取消后保留原笔记内容不变。

### 5.3 删除笔记

- 每条笔记悬浮或聚焦时显示删除入口。
- 点击删除时使用确认框防止误删。
- 删除成功后从列表移除。
- 如果删除的是正在编辑的笔记，应退出编辑态。

### 5.4 笔记展示

- 笔记列表展示标题、内容、更新时间。
- 时间使用相对时间，例如“刚刚”“1分钟前”“昨天”。
- 笔记内容保留换行展示。
- 长内容默认折叠。
- 长内容提供“展开 / 收起”操作。
- 笔记列表按更新时间倒序排列。
- 无笔记时展示“暂无笔记”空状态。
- 搜索无结果时展示“没有找到匹配的笔记”。

## 6. 任务功能需求

### 6.1 新建任务

- 点击“新建任务”后，系统直接创建一条空任务并进入编辑状态。
- 新任务默认位于“待完成”分组顶部。
- 输入任务内容后按 Enter 保存。
- 输入为空时按 Esc 或失焦取消，应移除该空任务。
- 保存后的任务默认未完成。

### 6.2 编辑任务

- 未完成任务支持点击编辑图标进入编辑状态。
- 任务支持双击进入编辑状态。
- 编辑时按 Enter 保存。
- 编辑时按 Esc 取消。
- 编辑内容为空时取消该编辑；如果是空草稿任务，则删除该任务。

### 6.3 删除任务

- 任务支持删除。
- 删除前使用确认框防止误删。
- 已完成任务仍保留删除能力。
- 删除后任务从当前分组移除。

### 6.4 完成状态

- 点击任务左侧圆形控件可切换完成状态。
- 未完成任务切换后进入“已完成”分组。
- 已完成任务再次点击后回到“待完成”分组。
- 已完成任务应弱化文字，并显示完成勾选态。

### 6.5 任务分组

- 任务列表包含“待完成”和“已完成”两个分组。
- 分组标题展示数量，例如“待完成（3）”“已完成（5）”。
- 待完成分组展示在已完成分组之前。
- 无任务时展示“暂无待办事项”。
- 搜索无结果时展示“没有匹配的任务”。

## 7. 搜索需求

- 搜索框根据当前模式显示不同占位文案：“搜索笔记”或“搜索任务”。
- 笔记模式搜索范围包括标题和内容。
- 任务模式搜索范围包括任务内容。
- 搜索应实时过滤当前模式列表。
- 清空搜索词后恢复完整列表。
- 切换模式时清空搜索词，避免跨模式误筛选。

## 8. 数据需求

### 8.1 笔记数据结构

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| title | string | 笔记标题 |
| content | string | 笔记正文 |
| createdAt | number | 创建时间戳 |
| updatedAt | number | 更新时间戳 |

### 8.2 任务数据结构

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| content | string | 任务内容 |
| completed | boolean | 是否完成 |
| createdAt | number | 创建时间戳 |
| updatedAt | number | 更新时间戳 |
| completedAt | number/null | 完成时间戳 |

### 8.3 持久化

- 当前版本使用 `localStorage` 保存统一应用状态。
- 保存内容包括笔记、任务、当前模式、置顶视觉状态、搜索词、展开状态、显示状态。
- 不持久化正在编辑的任务状态。
- 空任务草稿不应持久化。
- 兼容旧版本数据：`desktop-notes`、`desktop-todos`、`floating-note-state-v1`。
- 后续桌面端可通过 `window.noteStorage.load()` 和 `window.noteStorage.save(data)` 接管保存逻辑。

## 9. 非功能需求

### 9.1 易用性

- 新建后自动聚焦到主要输入框。
- 常见操作不应超过两次点击。
- 编辑入口既支持图标点击，也支持双击。
- 删除采用确认框，降低误删风险。

### 9.2 视觉一致性

- 继续保持 Figma 中的浅灰容器、白色卡片、深色主按钮、圆形图标按钮和分段切换样式。
- 笔记卡片和任务行在悬浮时显示操作按钮。
- 已完成任务需要弱化显示但仍保持可读。

### 9.3 数据可靠性

- 刷新或重新打开后数据不丢失。
- 旧版本本地数据应自动迁移到当前结构。
- 外部桌面存储接口失败时，应回退到 `localStorage`。

### 9.4 桌面端兼容

- 当前 `index.html` 可直接在浏览器打开。
- 后续可被 Electron 加载为桌面应用页面。
- 页面顶部区域提供拖拽兼容类，浏览器中无影响。
- 真正窗口置顶、隐藏到托盘、全局快捷键由 Electron 主进程实现，不在当前单文件前端中完成。

## 10. 验收标准

### 10.1 笔记验收

- 可以新建笔记并保存。
- 可以编辑已有笔记并保存。
- 可以双击笔记进入编辑。
- 可以删除笔记。
- 可以按标题或内容搜索笔记。
- 多行笔记内容能保留换行。
- 长笔记内容能展开和收起。
- 刷新页面后笔记仍存在。

### 10.2 任务验收

- 点击“新建任务”后直接出现可编辑空任务。
- 输入内容按 Enter 后保存任务。
- 空任务按 Esc 后移除。
- 可以编辑未完成任务。
- 可以双击任务进入编辑。
- 可以删除未完成和已完成任务。
- 可以完成任务和取消完成。
- 待完成、已完成数量正确。
- 刷新页面后任务仍存在。

### 10.3 通用验收

- 笔记 / 任务模式切换正常。
- 搜索框文案随模式变化。
- 关闭后可重新打开。
- `Ctrl+Shift+N` 可显示或隐藏应用。
- 置顶按钮有可见选中状态。
- 页面不依赖外部 CDN，离线可打开。

## 11. 后续阶段

后续如果要包装成真正桌面应用，建议新增 Electron 壳并实现：

- 本地 JSON 文件保存。
- 真实窗口置顶。
- 全局快捷键。
- 托盘显示 / 隐藏。
- 无边框透明窗口与系统级拖拽。
