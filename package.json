{
  "name": "floating-note-desktop",
  "version": "1.0.0",
  "description": "A lightweight floating notes and tasks desktop app.",
  "private": true,
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "check": "node --check frontend/app.js && node --check frontend/tauri-bridge.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
